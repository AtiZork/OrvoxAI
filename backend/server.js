/**
 * cPanel "Application startup file" entry point.
 * Build first: npm install && npm run build
 * Then set startup file to: server.js (with application root = this backend folder)
 */
require('./dist/main');
