# --- Option A: same domain (no subdomain) ---
# Leave empty so the site calls /api on orvoxai.com
NEXT_PUBLIC_API_URL=

# --- Option B: API on subdomain ---
# NEXT_PUBLIC_API_URL=https://api.orvoxai.com
